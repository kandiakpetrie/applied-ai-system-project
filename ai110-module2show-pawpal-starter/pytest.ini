[pytest]
# Two files share the basename test_pawpal.py (the root custom-harness script
# and tests/test_pawpal.py). importlib mode lets pytest collect both without a
# module-name clash, so a bare `python -m pytest` just works.
addopts = --import-mode=importlib
